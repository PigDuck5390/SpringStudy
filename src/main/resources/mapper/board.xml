<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.example.demo.mapper.BoardMapper">
  	<select id="findBoard" resultType="Board">
  		SELECT * FROM board ORDER BY bid DESC
  	</select>
  	<select id="BoardDetail" resultType="Board" parameterType="int">
  		SELECT * FROM board WHERE bid = #{bid}
  	</select>
  	<insert id="WriteBoard" parameterType="Board">
  		INSERT INTO board (title,text,id) VALUES (#{title},#{text},#{id})
  	</insert>
  	<update id="UpdateBoard" parameterType="Board">
  		UPDATE board SET title=#{title},text=#{text} WHERE bid=#{bid}
  	</update>
  	<delete id="DeleteBoard" parameterType="Board">
  		DELETE FROM board WHERE bid=#{bid}
  	</delete>
  </mapper>